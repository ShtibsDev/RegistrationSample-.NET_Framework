<UserControl x:Class="RegistrationSample.OldDesktopUI.CustomControls.PasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <PasswordBox x:Name="passwordBox" PasswordChanged="OnPasswordChanged">
        <!--<i:Interaction.Behaviors>
            <behavior:PasswordBoxBehaviorBinding Password="{Binding Password}"/>
        </i:Interaction.Behaviors>-->
    </PasswordBox>

    <!--<PasswordBox x:Name="passwordBox" PasswordChanged="OnPasswordChanged">

    </PasswordBox>-->
</UserControl>
